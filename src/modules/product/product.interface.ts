export interface IProduct {
  name: string;
  brand: string;
  price: number;
  model: string;
  inStock: boolean;
  quantity: number;
  createdAt?: Date;
  updatedAt?: Date;
}

// name, brand, price, model, stock
